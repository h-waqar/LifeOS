---
quick_id: "260911-1p0"
slug: "make-test-files-rule-and-relocate-tests"
date: "2026-09-11"
status: complete
---

# Quick Task Summary: 260911-1p0

**Task**: Establish global test file location rule and relocate tests to `scripts/tests/phase/plan/...`
**Status**: complete

## Accomplishments

1. **Established Global Project Rule & Conventions**:
   - Created `CONVENTIONS.md` mandating that all test files reside under `scripts/tests/{phase}/{plan}/...` (e.g. `scripts/tests/phase-01/plan-01/`) and prohibiting co-located test files inside `src/`.
   - Updated `AGENTS.md`, `.agents/GEMINI.md`, and `.planning/PROJECT.md` to document the global clean `src/` rule and centralized test hierarchy as authoritative constraints and conventions.
2. **Relocated All Test Files**:
   - `src/architecture-boundary.test.ts` -> `scripts/tests/phase-01/plan-01/architecture-boundary.test.ts`
   - `src/lib/crypto.test.ts` -> `scripts/tests/phase-01/plan-01/crypto.test.ts`
   - `src/lib/env.test.ts` -> `scripts/tests/phase-01/plan-01/env.test.ts`
   - Removed old test files from `src/`, leaving `src/` cleanly dedicated to production code.
3. **Updated Vitest Configuration**:
   - Reconfigured `vitest.config.ts` to discover tests at `scripts/tests/**/*.test.ts` and `scripts/tests/**/*.test.tsx`.
4. **Automated Clean `src/` Architectural Guard**:
   - Updated `scripts/tests/phase-01/plan-01/architecture-boundary.test.ts` to scan `src/` and assert that zero test files (`*.test.ts`, `*.test.tsx`, `*.spec.ts`, `*.spec.tsx`) are located within `src/`.
5. **Verification**:
   - `pnpm test`: 41/41 tests passing across 3 test suites.
   - `pnpm exec tsc --noEmit`: 0 errors.
   - `pnpm build`: production Next.js build passes cleanly.
