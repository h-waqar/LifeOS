# Quick Task Plan: 260911-1p0

**Task**: Establish global test file location rule and relocate current test files to `scripts/tests/phase/plan/...`
**Date**: 2026-09-11
**Type**: quick

## Overview

The user requested a clean repository structure where all test files are organized under `scripts/tests/{phase}/{plan}/...` rather than co-located inside `src/`. This plan establishes this architectural convention as a global project rule across project documentation and rules, relocates existing Plan 01-01 tests, reconfigures Vitest to discover tests in `scripts/tests/`, and adds an automated architecture test enforcing that `src/` contains zero test files.

## Tasks

- [ ] **Task 1: Global Project Rules & Conventions**
  - Define the global rule in `CONVENTIONS.md`, `AGENTS.md`, `.agents/GEMINI.md`, and `.planning/PROJECT.md`.
  - Rule: All test files must reside in `scripts/tests/{phase}/{plan}/...`. `src/` is strictly reserved for production code.
- [ ] **Task 2: Relocate Tests & Update Vitest Configuration**
  - Move `src/architecture-boundary.test.ts` -> `scripts/tests/phase-01/plan-01/architecture-boundary.test.ts`
  - Move `src/lib/crypto.test.ts` -> `scripts/tests/phase-01/plan-01/crypto.test.ts`
  - Move `src/lib/env.test.ts` -> `scripts/tests/phase-01/plan-01/env.test.ts`
  - Update `vitest.config.ts` to include `scripts/tests/**/*.test.ts` and `scripts/tests/**/*.test.tsx`.
- [ ] **Task 3: Update Test Implementations & Add Clean `src/` Assertion**
  - Update `scripts/tests/phase-01/plan-01/architecture-boundary.test.ts` to resolve `src/` using `path.resolve(process.cwd(), "src")`.
  - Add explicit test assertion to `architecture-boundary.test.ts` that scans `src/` and fails if any `*.test.ts`, `*.test.tsx`, `*.spec.ts`, or `*.spec.tsx` exists.
  - Update import paths in tests to use `@/lib/crypto` and `@/lib/env`.
- [ ] **Task 4: Full Verification**
  - Run `pnpm test` to ensure all 40+ tests pass.
  - Run `pnpm exec tsc --noEmit` to verify type checking.
  - Run `pnpm build` to verify production build.
